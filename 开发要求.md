# 开发要求

> 本文件为本项目的开发规范，所有新方案设计与代码编写均须遵循。
> 随时补充，补充后立即生效。

---

## 1. 代码架构

- 整洁、清晰、明了。
- 模块/函数职责单一，不重叠；分层清晰（如 store 的 action 之间职责互补而非交叉）。
- 命名表意，结构分区统一（注释块、导出顺序、文件组织保持一致风格）。
- 不引入与现有架构冲突的写法；改动尽量局部化，不动无关代码。

## 2. 高性能

- 方案设计阶段即评估时间/空间复杂度，优先选低复杂度实现。
- 避免嵌套全量遍历（如 O(N×M) 的逐项递归搜索），改用索引结构（Map/Set）做 O(1) 查表。
- 大数据结构遍历优先迭代而非递归（防栈溢出）；watcher 慎用 `deep: true`，引用变化触发即可时勿深监听。
- 高频路径（每次导航、每次渲染）零冗余；低频路径（初始化、登录）可适当摊销。
- 关键实现需在注释中标注复杂度与选型理由。

## 3. 注释

- 注释充足：公共函数、复杂逻辑、性能选型、易错点必须有注释。
- 注释说明「为什么」而非仅「是什么」；字段、参数、返回值含义清楚。
- 与现有注释风格统一（如 `/* ---------- 分区 ---------- */`、JSDoc 块注释）。
- 新增代码的注释要详细；改动代码同步更新注释，避免注释与代码脱节。

---

## 补充记录

<!-- 新增要求请按「## N. 标题」格式追加于此分隔线之下，并保留上方原有条目。 -->

## 4. 修改代码须先征得同意

- 任何修改代码的操作（含新增、删除、移动文件，改业务代码 / 配置 / mock 等）都必须先询问，得到明确同意后才能动手。
- 被问到问题时：只回答原因、结论，以及「如何实现」的方案；回答后不要直接开始改代码。
- 「帮我看看 / 为什么 / 解释下 / 排查下」视为只问不改；只有明确说「改吧 / 按这个方案调整 / 开始编写」等同意语之后才允许改。
- 本条对「把新要求写入本文档」这类用户明确要求的文档更新仍然适用：本次即用户明确要求写入，故可改本文件。
